{% block body %}
    {{ form_start(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}
    {{ form_errors(form) }}

    <a href="{{ path('period_index') }}">Back to the list</a>
{% endblock %}
